import { type } from "os";

export type LabelInput = {
    label: string,
    placeholder: string,
    icon: string
}

export type ProcessCardProps = {
    iconUrl: string,
    firstTitle: string,
    secondTitle: string,
    desc: string
}